UPDATE_PACKAGES = openssl libpcap daq snort httpd clamav coretree

UPDATE_ID = 010
UPDATE_NAME = update10

CLAMAV = yes

include ../Makefile.update

# If any files need to be moved or should be removed to avoid clobbering
#   customized files, create bash script 'pre-tar-setup' to perform the
#   work. It will be executed just before the distrib tree is stripped
#   and tarred.
